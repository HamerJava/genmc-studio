'use client';
import {useEffect,useRef} from 'react';
import {atlas,type Region} from '@/lib/skin/atlas';
import {pixelCanvas,type Skin} from '@/lib/skin/engine';
export default function AtlasView({skin,selected,labels,onPixel,onEnd}:{skin:Skin;selected?:{x:number;y:number;width:number;height:number};labels:boolean;onPixel:(x:number,y:number)=>void;onEnd:()=>void}){const ref=useRef<HTMLCanvasElement>(null);const down=useRef(false);useEffect(()=>{const c=ref.current;if(!c)return;const ctx=c.getContext('2d')!;ctx.clearRect(0,0,768,768);ctx.imageSmoothingEnabled=false;ctx.drawImage(pixelCanvas(skin.pixels),0,0,768,768);if(labels){for(const r of atlas(skin.model)){ctx.strokeStyle='rgba(70,90,65,.4)';ctx.lineWidth=1;ctx.strokeRect(r.x*12+.5,r.y*12+.5,r.width*12,r.height*12);ctx.fillStyle='#283c24';ctx.font='8px monospace';ctx.fillText(`${r.part.replace('_',' ')} ${r.face[0]}`,r.x*12+2,r.y*12+9)}}if(selected){ctx.strokeStyle='#ff7733';ctx.lineWidth=2;ctx.strokeRect(selected.x*12,selected.y*12,selected.width*12,selected.height*12)}},[skin,selected,labels]);function point(e:React.PointerEvent){const b=e.currentTarget.getBoundingClientRect();onPixel(Math.min(63,Math.max(0,Math.floor((e.clientX-b.left)/b.width*64))),Math.min(63,Math.max(0,Math.floor((e.clientY-b.top)/b.height*64))))}return <div className="atlas-wrap"><canvas ref={ref} width={768} height={768} aria-label="64 by 64 skin canvas" onPointerDown={e=>{down.current=true;e.currentTarget.setPointerCapture(e.pointerId);point(e)}} onPointerMove={e=>{if(down.current)point(e)}} onPointerUp={()=>{down.current=false;onEnd()}} onPointerCancel={()=>{down.current=false;onEnd()}}/></div>}
