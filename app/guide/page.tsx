import Link from 'next/link';
export default function Guide(){return <main className="guide"><Link className="logo" href="/">genMC(P)</Link><h1>A shared canvas for humans and agents.</h1><p>Open the studio in a browser that supports WebMCP. Your agent works in the same live draft as you. No provider key is stored by GenMC.</p><h2>Start with the map</h2><p>Ask the agent to call <code>get_skin_state</code> and <code>get_uv_atlas</code>. Every face has a name: <code>head.base.front</code>, <code>left_arm.overlay.back</code>, and so on. Left and right belong to the character, not the viewer.</p><h2>Make an edit</h2><pre>{JSON.stringify({expectedRevision:0,expectedContextRevision:0,label:'Golden detail',operations:[{type:'rectangle',region:'body.overlay.front',x:3,y:3,width:2,height:3,color:'#d6b869'}]},null,2)}</pre><p>Use the revision actually returned by your current skin. Local coordinates start at each face’s top-left. Base pixels must remain opaque; overlays may be transparent. One batch is one undo step.</p><h2>You stay in the loop</h2><p>Select a face in 3D or a rectangle in 2D. Play or pause the pose animation to inspect the skin. Use Mark area with Rectangle or Freehand and send a message to leave precise context; get_edit_context exposes both to your agent. Freehand refines the same marked area, with an Erase option to remove pixels. Drawing a new rectangle replaces the previous marks. Pass its revision as expectedContextRevision. When enabled, agent painting is restricted to your marks. The history filters distinguish your edits from agent edits, and the screen glows while a tool is running. Agent edits are immediately visible, reversible, and saved to your browser session’s private draft. Publishing requires confirming a fixed preview. Anyone may remix published skins.</p><h2>Queued → Read</h2><p>Sent messages appear above the input bar. They become Read when included in an actual WebMCP tool response. Every successful tool response includes new user actions and messages in <code>userUpdates</code>. To listen during the same turn, the agent can call <code>wait_for_user_action</code> with the latest <code>userUpdates.version</code>. The call waits up to 15 seconds. This cannot start an idle agent or interrupt its reasoning. Activity shows real function calls; Read means delivered, not completed.</p><h2>A mark, not a verdict</h2><p>Exports carry the GenMC v1 marker: redundant bits in the blue channel of base pixels. The change is at most one color value. It can be removed or copied and does not prove AI use, authorship, or authenticity. It is not Google SynthID.</p><h2>From pixels to worlds</h2><p>Skins are our first precise workspace for spatial AI: a small, inspectable canvas with a meaningful map. The longer-term vision is the same shared workflow for Minecraft structures, texture packs, and mods. Those capabilities are a roadmap, not current features.</p><p className="muted">Independent project. Not affiliated with Mojang or Microsoft. Your draft is tied to an anonymous browser cookie; clearing it loses access. Public skins and their lineage are visible to others.</p><Link href="/">Back to the studio ↗</Link></main>}
